PROGRAM HASH
VAR
	 SIZE, SUM, HASH, MOD, NUM : INTEGER
BEGIN
	STRLEN := 8;
	SIZE := 32;
	SUM := 0;

	FOR I := 1 TO STRLEN DO
		BEGIN
			READ(NUM);
			SUM == SUM + NUM
		END;
	MOD := SUM % SIZE;
	WRITE(MOD)
END.
